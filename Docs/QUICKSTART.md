# NOVA3D

```bash
cd ~/Desktop/NOVA3D && ./Tools/run.sh editor
```

Play (F5): мышь — обзор вокруг рыцаря, WASD — ходьба, F/ЛКМ — удар. Сверху HP. Звук в игре выключен.

Игра отдельно:

```bash
./Tools/run.sh knight
```
